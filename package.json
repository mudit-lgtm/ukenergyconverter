{
  "name": "uk-converter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --ip 0.0.0.0 --port 3000",
    "build": "echo 'Static site - no build needed'",
    "deploy": "wrangler pages deploy public --project-name uk-converter"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
